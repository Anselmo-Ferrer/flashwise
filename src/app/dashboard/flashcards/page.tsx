'use client'

import FlashcardFlip from "@/components/FlashcardFlip";

export default function FlashcardsPage() {
  return (
    <div>
      <FlashcardFlip front="O que é React?" back="Uma biblioteca JavaScript para interfaces." />
    </div>
  )
}